# configs/config.yaml
defaults:
  - base
  - model: unet
  - dataset: isic2018
  - inspect: default
  - logging: mlflow
  - train: default
  - eval: default
  - _self_

project:
  # I/O
  batch_size: 32
  image_size: 224
  augmentation: True
  # Dataloaders
  num_workers: 1
  # Setup
  device: cuda
  seed: 42
  # Training
  epochs: 300
  use_pretrained_ckpt: False

  env: ${paths.repo_root}/environment.yml
