# configs/config.yaml
defaults:
  - model: resnet_unet
  - dataset: isic2017
  - task: binary
  - inspect: default
  - logging: mlflow
  - train: default
  - eval: default
  - _self_

project:
  name: unet_variants
  classifier: seg
  device: cuda
  seed: 42
  image_size: 224
  batch_size: 32
  num_workers: 8
  epochs: 100
  use_pretrained_ckpt: False

# NEW: paths group (works everywhere)
paths:
  # repo root when running scripts (Hydra may chdir; we keep chdir false so cwd stays stable)
  repo_root: ${oc.env:UNET_VARIANTS_ROOT}
  data_dir: ${paths.repo_root}/data
  pretrained_ckpt: ${paths.repo_root}/pretrained_ckpt/${model.pretrained_ckpt}
  runs_dir: ${paths.repo_root}/runs
  onnx_dir: ${paths.runs_dir}/onnx

hydra:
  run:
    dir: ${paths.runs_dir}/hydraruns/${now:%Y-%m-%d}/${now:%H-%M-%S}_${model.name}_${dataset.name}
  job:
    chdir: false
