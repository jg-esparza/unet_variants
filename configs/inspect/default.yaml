# Configs model inspection
name: inspect

batch_size: 1
in_channels: ${dataset.in_channels}
image_size: ${dataset.image_size}
export_path: ${paths.runs_dir}/hydraruns/${now:%Y-%m-%d}/${now:%H-%M-%S}_${model.name}_${dataset.name}
torchinfo_summary:
    print_summary: True
    export_summary: False
    export_path: ${inspect.export_path}/summary_${model.name}.txt

flops_thop:
  print_report: True
  export_report: False
  save_txt: ${inspect.export_path}/flops_${model.name}.txt
  save_json: ${inspect.export_path}/flops_${model.name}.json

export_onnx:
  enable: True
  path: ${paths.onnx_dir}/${model.name}_${dataset.in_channels}_${dataset.image_size}.onnx
  view: False
  port: 8081
